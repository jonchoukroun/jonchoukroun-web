<div class="d-flex flex-row align-items-center justify-content-between py-1">
  <div class="d-flex flex-row align-items-center justify-content-left m-0 p-0">
    <div class="header-icon">
      <img src={{iconSource}} alt={{iconAlt}}>
    </div>
    <p>{{windowTitle}}</p>
  </div>

  <div class="flex-row align-items-end mr-1">
    {{#if showMinimizeButton}}
      <button
        type="button"
        data-toggle="collapse"
        data-target="#{{cardBodyId}}"
        class="btn btn-primary btn-sm p-0"
      >
        <div class="btn-inner px-1">
          <i class="fas fa-window-minimize"></i>
        </div>
      </button>
    {{/if}}

    {{#if showCloseButton}}
      <button
        type="button"
        class="btn btn-primary btn-sm p-0"
        {{action "closeWindow"}}
      >
        <div class="btn-inner px-1">
          <i class="fas fa-times"></i>
        </div>
      </button>
    {{/if}}
  </div>
</div>
